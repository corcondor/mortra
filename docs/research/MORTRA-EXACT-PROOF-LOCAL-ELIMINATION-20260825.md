# MORTRA 固定89問の厳密局所消去実験（2026-08-25）

> 追補: 本記録後、公式JGEXの既知交点除外を復元して
> `2024PlanetCupp10`を追加認証した。最新値は **60/89**。
> 詳細は `MORTRA-LINE-CIRCLE-KNOWN-ROOT-EXCLUSION-20260825.md` を参照する。

## 目的

HAGeoの凍結済み89問だけを母集団とし、未認証30問の厳密多項式証明を高速化する。
問題ID、期待解答、外部LLM、問題固有の分岐は使わず、全問題へ共通に適用できる
局所消去と証明書保存だけを変更する。追加得点は、余り0の証明書を独立再生できた場合に限る。

## 原理

座標化された幾何問題の最終証明は、多項式イデアルへの目標式の所属判定になる。
大域的なGroebner基底を最初から計算する代わりに、局所的に現れる変数を先に消去し、
より小さな多項式環へ射影する。ただし射影ごとに次を保存する。

- 入力多項式と出力多項式
- 入力の線形結合が出力になることを示すイデアル所属証人
- 必要な非零条件
- 再生余り0
- 証明内容のSHA-256

線形な2式

```text
a*x + b = 0
c*x + d = 0
```

から `x` を消去する場合、一般のSylvester行列や拡張GCDは不要である。恒等式

```text
a*(c*x+d) - c*(a*x+b) = a*d-b*c
```

を直接使えば、除算も追加の非零仮定もなく、厳密なresultant射影を得られる。

## 方法

1. 線形resultantを上の2x2行列式へ置換した。
2. 項数上限を、因数分解された表層の項数ではなく、残る変数について展開した場合の上限で判定した。
3. 消去後の正規化を、既に消した変数や係数だけの幾何chartを除いた活性環に限定した。
4. 残差条件付き順位を使わない実行で、未使用の残差メッセージを毎段再因数分解していた処理を停止した。
5. 完全な途中証明はSHA付きsidecarへ一度だけ保存し、進捗JSONには件数と参照先だけを残した。
6. 既に正規化済みの非退化因子積をbeam候補ごとに再因数分解せず、因子キー集合で重複を除いた。
7. `2023VietnamTSTp3`を同じ入力・同じF5B・同じ固定語彙でA/B測定した。
8. 一次消去と二次消去を比較し、最終多項式環が小さくなるか測った。
9. `2024PlanetCupp10`の深さ4飽和探索を、因子積再分解の有無だけ変えてA/B測定した。
10. 未使用変数だけが消えた場合も、縮約後の環を最終Groebnerへ渡すよう修正した。
11. `2017G4`で項数上限256と4096を比較し、共通の厳密局所射影が最終環を縮小するか測った。
12. ゴール指向局所縮約をWolfram証明探索へ接続し、Wolframの結果も元方程式への商多項式再生で判定した。
13. Wolframへ渡す係数chartを因数形のまま保持し、飽和を使わない実行では未使用の非退化因子分解を省いた。

## 結果

### 回帰試験

- 局所消去、厳密制約bridge、Wolfram bridge、実験runnerの重点試験: **104/104成功**
- 型付き探索と89問集計を含む広域回帰試験: **181/181成功**
- すべての局所消去stepで、保存したイデアル所属証人の再生余りは0

### `2023VietnamTSTp3` の前処理

| 実装段階 | Groebner開始 | 消去変数 | 最終変数 | 最終式 |
|---|---:|---:|---:|---:|
| 旧terms256 | 120.280秒 | 2 | 5 | 4 |
| 直接線形resultant | 43.135秒 | 2 | 5 | 4 |
| 活性環正規化 | 32.271秒 | 2 | 5 | 4 |
| 未使用残差再計算の除去 | 9.523秒 | 2 | 5 | 4 |
| SHA sidecar進捗 + 二次許可 | **3.858秒** | 2 | 5 | 4 |

同じ最終環へ到達する前処理は、120.280秒から3.858秒へ **約31.2倍**高速化した。
進捗JSONは52,455 byteから16,588 byteへ縮小し、完全な途中証明2件はSHA付きsidecarへ保持した。

### 厳密証明の成否

- 一次局所消去、600秒: timeout、追加証明0
- 二次まで許可、180秒: timeout、追加証明0
- 二次許可でも消去変数数と最終環は一次と同じ
- 現在の厳格得点: **59/89**（変更なし）

### `2024PlanetCupp10` の飽和探索

| 実装 | 600秒内の状態 | 最終結果 |
|---|---|---|
| 因子積を候補ごとに再因数分解 | 深さ3の候補開始でtimeout | 未完了 |
| 正規化済み因子を未展開積で保持 | **145.241秒で全候補完了** | unproved |

旧実装は600秒で完了しなかったため、完了時間は少なくとも **4.1倍**改善した。
修正後は深さ1で3候補、深さ2で3候補、深さ3で1候補の合計7候補を厳密還元した。
ただし、どの非退化因子積も元の余りより良い正規形を作らず、最良状態は乗数1の初期余りだった。
これは時間打切りではなく、今回の有限候補集合を完走した負の結果である。

### `2017G4` の最終環縮小

| 経路 | Groebner開始 | 最終変数 | 最終式 | 600秒の結果 |
|---|---:|---:|---:|---|
| 修正前 | 3.105秒 | 11 | 9 | timeout |
| 未使用変数の伝播修正 | 7.397秒 | 9 | 9 | timeout |
| 項数上限4096 | 134.930秒 | **7** | **7** | timeout |

項数上限4096では、未使用変数2個に加えて `_free_y_15` と `_free_x_14` を
厳密なresultant/localizationで消去した。2件の非自明な局所射影はともに
イデアル所属証人を持ち、再生余り0、SHA-256一致である。
ただし、7変数・7式のF5Bも600秒では閉じず、追加認証は0だった。

### Wolfram独立探索

同じゴール指向縮約をWolframへ接続した。係数chartをPython側で完全展開していたため
Wolfram起動前にメモリが増大する不具合を修正し、128,723 byteの因数形入力として渡した。

| 探索 | 前処理後 | Wolfram上限 | 結果 |
|---|---:|---:|---|
| ExtendedGroebnerBasis + PolynomialReduce | 7変数・7式 | 600秒 | right-censored timeout |
| 元7式への直接PolynomialReduce（4順序） | 7変数・7式 | 180秒 | right-censored timeout |

直接還元実験の局所前処理証明SHA-256は
`b31a11d01e0fcb71dd603f499f3ab181d6fc30a941691af65359425dadc30108` で、
局所射影は再生済みである。どちらのWolfram探索も商証明書を返さなかったため、
`True` や数値一致だけを根拠に正答へ加えていない。

## 考察

今回の変更は計算を速くしたが、数学的到達可能性は増やしていない。
`2023VietnamTSTp3`では、局所消去が支配項だった状態を解消した後も、
5変数・4式・係数パラメータ3個のGroebner計算が600秒で閉じなかった。
したがって次の課題は、単に時間を延ばすことではなく、厳密な中間補題または追加の局所射影によって
最終環そのものを小さくすることである。

二次resultantを許可しても環が小さくならなかったため、次数上限だけを増やす方針はこの問題では支持されない。
また、高速化と得点改善を同じ成果として扱わない。証明書が閉じていない限り59/89のままである。

`2024PlanetCupp10`では、再因数分解を除くことで探索の未完了を解消したが、非退化因子の積だけでは
目標余りを閉じないことまで確定した。次に必要なのはbeam幅や時間の増加ではなく、
現在の余りを別の型付き幾何関係へ戻す中間補題、または最終環を縮小する追加の厳密射影である。

`2017G4`では最終環を11変数から7変数へ縮小しても、SymPyとWolframの双方で証明が閉じなかった。
したがって「変数を減らせばこの問題が直ちに解ける」という仮説は支持されない。一方で、
高項数の局所射影が共通規則だけで環をさらに2変数縮小できることは確認できた。
次は、この縮約成果物を再利用可能な証明checkpointとして保存し、同じ前処理をCASごとに
繰り返さず、残る7変数環に対する中間補題分割または別の厳密消去順序を比較する。

## 結論

全89問に適用できる厳密な局所消去の共通実装を高速化し、途中証明を失わずに
前処理を約31.2倍短縮し、別問題では600秒超の探索を145.241秒で完走させ、
`2017G4`の最終環を11変数から7変数へ縮小した。しかし追加正答はまだ0で、
固定89問の厳格得点は **59/89** のままである。
次の実験は、固定89問のうち既に最終還元・飽和探索まで進んだ未認証問題を対象に、
保存済み停止段階から証明を継続し、追加の厳密証明が得られるかを測る。

## 成果物

- 一次600秒: `data/jgex-exact-local-fastpath-600s-2023vietnam-2026-08-25.json`
- 二次180秒: `data/jgex-exact-local-degree2-180s-2023vietnam-2026-08-25.json`
- 一次進捗: `data/jgex-exact-local-fastpath-600s-2023vietnam-runs/2023VietnamTSTp3.progress.json`
- 二次進捗: `data/jgex-exact-local-degree2-180s-2023vietnam-runs/2023VietnamTSTp3.progress.json`
- 二次途中証明: `data/jgex-exact-local-degree2-180s-2023vietnam-runs/2023VietnamTSTp3.progress.checkpoints/`
- 旧飽和600秒: `data/jgex-exact-factorized-saturation-600s-2024planet-2026-08-25.json`
- 修正後飽和: `data/jgex-exact-no-refactor-saturation-600s-2024planet-2026-08-25.json`
- `2017G4` 11変数対照: `data/jgex-exact-local-fastpath-600s-2017g4-2026-08-25.json`
- `2017G4` 9変数対照: `data/jgex-exact-unused-ring-fix-600s-2017g4-2026-08-25.json`
- `2017G4` 7変数処置: `data/jgex-exact-local-term4096-600s-2017g4-2026-08-25.json`
- Wolfram拡張基底: `data/wolfram-goal-local-term4096-2017g4-2026-08-25.json`
- Wolfram直接還元: `data/wolfram-goal-local-term4096-direct180-2017g4-2026-08-25.json`
