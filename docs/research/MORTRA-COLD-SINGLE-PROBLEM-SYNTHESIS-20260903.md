# MORTRA cold single-problem synthesis, 2026-09-03

## 目的

登録済みの問題番号、保存済み解答、完成済み Atlas 経路を使わず、現在入力された問題文だけから実行可能な証明プログラムを合成する。複数設問では、各設問を独立に認証した後、全証明書の論理積として一つの解答へ戻す。

## 仮説

1. 未解決の一部は数学知識の不足ではなく、LaTeX の量化記号・添字・複数設問をスカラー式として誤読する入口の問題である。
2. 問題固有の解答を登録する代わりに、素数の偶奇分解、円分多項式、一次分数変換という少数の可逆な構造を実装すれば、異なる係数・角度にも再利用できる。
3. 複数設問の正答は、各子証明書の問題文ハッシュ・解答ハッシュ・証明書ハッシュを再検査し、全設問の証明がそろった場合にだけ認める必要がある。

## 方法

凍結した90問の問題文のみを使用した。問題文集合の SHA-256 は `0eb15110cc258b2cd406fdef98a394b4a159861cf2c59fec9d08a722e823fa7c` である。

監査条件は次のとおり。

- 期待解答を与えない。
- 解説を与えない。
- 外部 LLM を使わない。
- 登録済み定理核を無効にする。
- 厳密計算、独立再生、問題文と解答の SHA-256 一致をすべて必須にする。
- 登録済み複合経路による6問は総認証数には含めるが、登録非依存数には含めない。

比較元は `artifacts/benchmarks/cold-single-problem-coverage-triangle-radii-exponential-hash-bound-20260903.json`、最終結果は `artifacts/benchmarks/cold-single-problem-coverage-mobius-polynomial-transport-20260903.json` である。ファイル SHA-256 は順に `FA750C3334F8A91EE4735A0993846DED817473034AEC6495329050605B13B6B7`、`0F6271AC0017C09700DB599E8961093FA8C658B99BAC6AFE72F239E10CE4C38D`。

## 実装した共通構造

### 型付き字句境界

積分、和、極限、角、複数設問、ギリシャ文字、添字を保存し、未展開の束縛式や図形関係を一般の一変数方程式へ誤送出しないようにした。

### 素数の偶奇分解

素数逆二乗和では、2の項と奇素数全体を分離し、奇整数全体による上界を現在入力の指数と比較する。素数辺三角形では Heron 判別式

\[
D=(a+b+c)(-a+b+c)(a-b+c)(a+b-c)
\]

を、奇素数三辺、2を一辺だけ含む場合、2を二辺含む場合、三辺とも2の場合に完全分割し、各場合で `D` が平方数でないことを証明する。

### 円分多項式と実根分離

`cos(r pi)` を1の冪根の実部として表し、円分多項式との消去から最小多項式を作る。有理化では分母の逆元を最小多項式を法として計算する。小数丸めでは全実根を有理区間で分離し、余弦の順序で対象根を選び、区間全体が同じ丸め区間に入ることを証明する。

### 多項式、一次分数変換、固定点

`alpha=cos(2 pi/n)` の最小多項式 `f` と `S=1/(1-x)` を現在入力から抽出する。まず、与えられた `f` が指定余弦の最小多項式と定数倍を除いて一致することを確認する。次に

\[
P(S)=S^d f(1-S^{-1})
\]

を展開し、`P(S)=0` を `S=g(S)` へ可逆に整理する。定数項は `S` を無限大へ送った極限として抽出する。収束率は固定点で微分し、`c=cos(pi/n)` の最小多項式を法として高次項を消去し、代数的数の符号を厳密に判定して絶対値を取る。

### 複数設問の合成

各設問は共有条件を継承した別の型付き義務として解く。親証明書は、全子証明書の問題文 SHA-256、解答 SHA-256、証明書 SHA-256 を再計算し、一つでも不一致なら親問題を不認証にする。

## 結果

| 指標 | 比較元 | 最終 | 差 |
|---|---:|---:|---:|
| 認証済み | 25/90 | 29/90 | +4 |
| 登録非依存 | 19/90 | 23/90 | +4 |
| 登録済み複合再利用 | 6/90 | 6/90 | 0 |
| 未解決 | 65/90 | 61/90 | -4 |

新たに認証した問題は次の4問だけであり、既存25問の後退はなかった。

| 問題 | 共通構造 | 証明書 SHA-256 |
|---:|---|---|
| 5 | 円分消去、有理化、実根分離、複数設問合成 | `111484721e0c56318142801b72c44bb333e09a6be407b61a65b947ddf5edad0a` |
| 18 | 最小多項式、一次分数変換、固定点微分、複数設問合成 | `daa4f3cb1b4c78b3dce49acd98577dc5967d855b0976d48452b6c16d7f6a0858` |
| 22 | 素数逆冪和の偶奇分解と厳密上界 | `5df9546a798ac4abe6de0d7990d1e58917ddb07a71ae9f0a3cd9c332f3b67c32` |
| 33 | Heron 判別式の全偶奇場合分け | `84a6ad78acca58b59cfb2d0eaa2356b61d0101d17fab70d9458f4ad15df08046` |

問題18の最終結果は

\[
C_0=10,
\]

\[
k=2\left(1-\cos\frac{\pi}{11}\right)
\left(8\cos^3\frac{\pi}{11}+4\cos^2\frac{\pi}{11}
-4\cos\frac{\pi}{11}-1\right).
\]

入力を `2 pi/5` と最小多項式 `4x^2+2x-1` へ変えると、同じコードが `C_0=2` と `k=(3-sqrt(5))/2` を再計算した。一方、最小多項式をその根を含むだけの三次式 `4x^3-10x^2-7x+3` へ変えた場合と、要求する半角だけを `pi/7` へ変えた場合はいずれも不認証になった。

全関連テストは `407 passed in 95.97s`。問題43で一度発生した対象外タプル入力の例外は、スカラー型以外を入口で拒否した後の90問再実行で0件になった。

## 考察

今回の4問増は、解答文字列の追加ではない。各増分は現在入力の係数、角度、指数、目標値から証明プログラムを再構成し、値を変えた問題でも再計算する。ただし、これは任意の数学問題を扱えることを意味しない。現時点での実証範囲は90問中29問、登録非依存では23問であり、未解決は61問残る。

問題18は、旧経路では登録済み定理として解けていた。今回の差分は答えを新しく知ったことではなく、その計算核を登録名から切り離し、現在の問題文から到達できるようにした点にある。これにより、同じ構造を持つ別の円分位数と係数へ適用できる。

## 結論

型付き入口、少数の可逆チャート、子証明書の論理積という三層を組み合わせることで、凍結90問の登録非依存認証率を `19/90` から `23/90` へ上げた。次の改善対象は、未解決61問を問題番号ではなく停止した型付き義務で分類し、複数問を同時に閉じる最小の共通変換を選ぶことである。

## 公開記録

同日に、MORTRAが根集合の合成から生成し、根と係数を厳密検証した三次方程式の新作問題を `@MORTRA_AI` へ公開した。

- <https://x.com/MORTRA_AI/status/2095419519963652343>
