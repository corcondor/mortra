-r requirements-test.txt
# Exact bridge imports Newclid's construction schemas, but calls no external prover.
# Same unmodified local snapshot as requirements-geometry.txt. Run from repo root.
./vendor/newclid
