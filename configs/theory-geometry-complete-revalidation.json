{
  "domain": {"kind": "geometry", "mode": "complete_revalidation"},
  "protocol": {
    "python_hash_seed": 0,
    "purpose": "Re-run acquisition and selection without tuple or per-family candidate truncation.",
    "evaluation_status": "All goal templates and coordinate-generation seeds are now regression data; no new generalization claim.",
    "library_source": "Only C-archive generated earlier in this same invocation. No previous acquired definitions."
  },
  "acquisition_config": "configs/theory-geometry-semantic-feedback-eight-cycles.json",
  "selection_config": "configs/theory-geometry-selection-factorial.json",
  "search_overrides": {
    "candidate_enumeration": "complete",
    "ranking_window": 16,
    "max_candidate_checks": 100000,
    "trace_predicates": true
  }
}
